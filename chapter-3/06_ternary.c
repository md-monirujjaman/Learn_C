#include <stdio.h>

int main()
{

    // condition ? expression-if-true : expression-if-false
    int a = 44;
    int b = 55;
    a > b ? printf("a is gatherthen") : printf("b is less then");
    return 0;
}