#include <stdio.h>

int main (){
    int r = 6;
    int height =30;
    printf("the area of circle with radius %d is %f\n",r,3.14*r);
    printf("the vloume of cylinde rwith radius %d and height %d is %f",r,height,3.14*r*r*height);
    return 0;
}