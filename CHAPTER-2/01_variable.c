#include <stdio.h>

int main (){
     int i =10; //Delare and intiialize 'i' with 10
     int j =i;
     // %d is called formato specifier
     // %d is for int , %f ir for float and %c is for char
     int a =2, b=3, c=4, d=5;
     printf("the value of i is %d and the value of j is %d\n",i,j);
     printf("the value of a is %d and the value of b is %d\n",a,b);
     printf("the value of c is %d and the value of d is %d\n",c,d);
    return 0;
}